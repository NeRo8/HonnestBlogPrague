const initialState = {
  layoutCash: [{id: 0, name: 'One'}],
};

const layoutsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default layoutsReducer;
